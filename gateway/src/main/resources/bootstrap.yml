spring:
  application:
    name: gateway
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8765/eureka
