server:
  port: 9100

spring:
  application:
    name: tracer
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8765/eureka
