server:
  port: 9998

spring:
  application:
    name: store
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8765/eureka
